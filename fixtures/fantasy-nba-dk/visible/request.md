Choose the best legal NBA DraftKings-style lineup from this roster for the
provided slate. Use only the visible pre-game data. Save your final lineup to
`/workspace/recommendation.json` with a `lineup` array of three player IDs in
slot order: PG, F, UTIL. Also save a concise explanation to
`/workspace/recommendation.md`. Create `/workspace/fantasy.sqlite` and record
the recommendation in a `recommendations` table, including the slate's
`as_of` timestamp and the selected lineup.
